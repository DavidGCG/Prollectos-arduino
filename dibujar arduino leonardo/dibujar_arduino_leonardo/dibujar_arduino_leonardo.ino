

const int upButton = 2;     

void setup() { 
  pinMode(upButton, INPUT);       
  Mouse.begin();
}

      
void loop() {
 

  if (digitalRead(upButton) == HIGH) {
   /* 
   ///abajo
   for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(0, 1);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }

    ///derecha   
     for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(1, 0);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
     ///ariba     
     for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(0, -1);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
       ///izquierda   
      for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(-1, 0);
            //Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
 */
 
 
    
   
     ///ariba     
     for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(0, -1);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
  

    ///derecha   
     for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(1, 0);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
          ///abajo
   for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(0, 1);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }

    ///derecha   
     for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(1, 0);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
     ///ariba     
     for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(0, -1);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
  

    ///derecha   
     for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(1, 0);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
     
     ///abajo
   for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(0, 1);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }

    ///derecha   
     for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(1, 0);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
     ///ariba     
     for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(0, -1);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
  

    ///derecha   
     for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(1, 0);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
          ///abajo
   for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(0, 1);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
    for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(0, 1);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
          
          for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(0, 1);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
          ///izquierda   
      for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(-1, 0);
            //Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
          ///izquierda   
      for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(-1, 0);
            //Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
          ///izquierda   
      for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(-1, 0);
            //Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
          ///izquierda   
      for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(-1, 0);
            //Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
          ///izquierda   
      for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(-1, 0);
            //Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
   ///ariba     
     for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(0, -1);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
          ///ariba     
     for (int i=0; i<200; i++) // declara i, prueba que es menor que
        {
            Mouse.click(MOUSE_LEFT);
            Mouse.move(0, -1);
            Mouse.click(MOUSE_LEFT);
            delay(1); // espera ¼ seg.
          }
     
     
   
 
  }

}



